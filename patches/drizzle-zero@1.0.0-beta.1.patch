diff --git a/dist/index.cjs b/dist/index.cjs
index 3a164023730d29705e8dc09c16700cf3a36aad42..ab78801c01324b02abea15074028cc238ee2b230 100644
--- a/dist/index.cjs
+++ b/dist/index.cjs
@@ -403,13 +403,14 @@ var normalizeBetaRelation = ({
     table: relation.targetTable,
     fallbackTableName: relation.targetTableName
   });
-  const sourceFieldNames = relation.sourceColumns.map(
+  const unwrapRelationColumn = (column) => column && typeof column === "object" && "_" in column && column._ && typeof column._ === "object" && "column" in column._ ? column._.column : column;
+  const sourceFieldNames = relation.sourceColumns.map(unwrapRelationColumn).map(
     (column) => getDrizzleColumnKeyFromColumnName({
       columnName: column.name,
       table: (0, import_column.getColumnTable)(column)
     })
   );
-  const targetFieldNames = relation.targetColumns.map(
+  const targetFieldNames = relation.targetColumns.map(unwrapRelationColumn).map(
     (column) => getDrizzleColumnKeyFromColumnName({
       columnName: column.name,
       table: (0, import_column.getColumnTable)(column)
diff --git a/dist/index.js b/dist/index.js
index eabce7f22a3b5b77d00992d530f4e9cb8c4a09e8..97ad1019eab8a7ccaaab3ee560a378ff2b574858 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -382,13 +382,14 @@ var normalizeBetaRelation = ({
     table: relation.targetTable,
     fallbackTableName: relation.targetTableName
   });
-  const sourceFieldNames = relation.sourceColumns.map(
+  const unwrapRelationColumn = (column) => column && typeof column === "object" && "_" in column && column._ && typeof column._ === "object" && "column" in column._ ? column._.column : column;
+  const sourceFieldNames = relation.sourceColumns.map(unwrapRelationColumn).map(
     (column) => getDrizzleColumnKeyFromColumnName({
       columnName: column.name,
       table: getColumnTable(column)
     })
   );
-  const targetFieldNames = relation.targetColumns.map(
+  const targetFieldNames = relation.targetColumns.map(unwrapRelationColumn).map(
     (column) => getDrizzleColumnKeyFromColumnName({
       columnName: column.name,
       table: getColumnTable(column)
